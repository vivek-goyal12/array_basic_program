package practice;

import java.util.Scanner;

public class InsertElement {
    public static void main(String[] args) {
        int n; // size of array
        Scanner sc = new Scanner(System.in);
        n = sc.nextInt();
        int []c = new int[n]; //  create an array
        for(int i = 0 ; i<n ; i++){
            c[i]= sc.nextInt(); // input elements from user
        }
        for(int i = 0 ; i<n ; i++)
        System.out.print(c[i]+" ");// print all elements in one line
    }
}
